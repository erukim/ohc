// @ts-nocheck
/* eslint-disable no-unused-vars */

// Events
import { Event } from '../base/Event'
import Embed from '../../utils/Embed'
import { Events } from 'discord.js'

export default new Event(Events, async (client) => {
})
